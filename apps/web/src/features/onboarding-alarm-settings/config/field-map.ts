export const ALARM_SETTINGS_FIELD_MAP = {
  interval: 'intervalMinutes',
  activeStartAt: 'activeStart',
  activeEndAt: 'activeEnd',
  focusStartAt: 'focusStart',
  focusEndAt: 'focusEnd',
  repeatDays: 'weekdays',
} as const;
